import 'package:flutter_getx_structure/core/constants/app_imports.dart';

class AppBindings extends Bindings {
  @override
  void dependencies() {
    // Initialize global dependencies here if any
    // For example:
    // Get.lazyPut<ApiClient>(() => ApiClient());
  }
}
